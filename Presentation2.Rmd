---
title: "Presentation"
output: html_document
date: "2024-08-26"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(ggplot2)
library(data.table)
library(metR)
library(lubridate)
library(unglue)

source("scripts/help_functions.R")

map <- rnaturalearth::ne_coastline(scale = 50, returnclass = "sf")

theme_timeseries <- list(
  scale_color_manual(values = c("#1B9E77", "#D95F02", "#7570B3", "#E7298A", "#66A61E", "black")),
  scale_linewidth_manual(values = c(0.5, 0.8)),
  guides(color = guide_legend(nrow = 2), linewidth = "none"),
  scale_x_continuous(breaks = seq(0, 20, 2)),
  coord_cartesian(xlim = c(0, 15), expand = FALSE),
  theme_minimal(),
  theme(legend.position = "bottom", legend.box="vertical",
        panel.grid.minor = element_line(color = "white"), aspect.ratio = 1/3,
        panel.grid.major.y = element_line(color = "white"))
)
```

## Strong volcanos

```{r}
so2 <- readr::read_lines(here::here("data/MSVOLSO2L4_v04-00-2024m0129.txt"), skip = 48)  |>
  gsub(" +\\t", "\t", x = _) |>   # Líneas con espacios Y tab
  gsub(" +", "\t", x = _) |>      # Líneas con espacios
  gsub("\\t$", "", x = _) |>      # Tab extra al final
  fread(text = _, na.strings = c("nd", "-999")) |> 
  janitor::clean_names() |> 
  _[, date := lubridate::make_datetime(yyyy, mm, dd, tz = "UTC")]

strong <- so2[, ini_date := group_eruptions(date, 60), by = volcano] |> 
  _[, p_alt_obs := fifelse(is.na(p_alt_obs), p_alt_est ,p_alt_obs)] |> 
  _[, .(p_alt_obs = max(p_alt_obs, na.rm = TRUE),
        so2_kt = sum(so2_kt, na.rm = TRUE),
        lat = unique(lat),
        lon = unique(lon),
        vei = max(vei, na.rm = TRUE)), by = .(volcano, ini_date)] |>
  # _[, let(base_t = calculate_tbase(global_mean, ini_date)),
  #   by = .(volcano, ini_date)] |>
  _[, let(id = paste0(volcano, "_", ini_date))] |> 
  _[(so2_kt > 900 & p_alt_obs > 15 & vei >= 5) | stringr::str_detect(volcano, "Hunga_Tonga") & p_alt_obs > 15] |> 
  _[]

strong |> 
  ggplot(aes(lon, lat)) +
  geom_sf(data = map, inherit.aes = FALSE, fill = NA, color = "grey") +
  geom_point(aes(fill = p_alt_obs, size = so2_kt), shape = 21) +
  geom_label(aes(label = volcano), nudge_y = 12, nudge_x = 30) +
  scale_fill_viridis_c(guide = guide_colorbar(barwidth = 10,
                                              barheight = 0.5)) +
  labs(x =  NULL, y = NULL, fill = "Plume\nAltitude (Km)", size = "SO2 (kt)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```


```{r}
so2 |> 
  ggplot(aes(as_date(date), so2_kt)) +
  geom_point(aes(color = factor(vei))) +
  geom_point(data = strong, aes(x = as_date(ini_date)), shape = 21, fill = NA, size = 3) +
  ggrepel::geom_label_repel(data = strong, aes(x = as_date(ini_date), label = volcano)) +
  scale_color_viridis_d(option = "inferno", direction = -1) +
  scale_y_log10(labels = scales::trans_format("log10", scales::math_format(10^.x))) +
  labs(x = "Date", y = "Measured SO2 mass in kilotons",
       color = "Explosivity index") +
  theme_minimal() +
  theme(legend.position = "bottom")
```


## Effect on 2m temperature

### Anomalies

```{r}
# t2_mean <- readr::read_rds("data2/2t_mean_daily_regions.rds")$global|> 
#   _[, time := as_date(floor_date(time, "day"))] |> 
#   _[, t2m_notrend := pracma::detrend(t2m)] |> 
#   _[, t2m_a := t2m_notrend - mean(t2m_notrend), by = .(day(time), month(time))]

t2_mean <-  ReadNetCDF("data2/2t_daily_global_anomaly.nc", vars = "t2m") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("t2m", "t2m_a")

# tropics_t2_mean <- readr::read_rds("data/t2_mean_daily_regions.rds")$tropics |> 
#   _[, time := as_date(floor_date(time, "day"))] |> 
#   _[, t2m_notrend := pracma::detrend(t2m)] |> 
#   _[, t2m_a := t2m_notrend - mean(t2m_notrend), by = .(day(time), month(time))]

tropics_t2_mean <-  ReadNetCDF("data2/2t_daily_tropics_anomaly.nc", vars = "t2m") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("t2m", "t2m_a")


australia_t2_mean <-  ReadNetCDF("/g/data/w40/pc2687/ve/2t_daily_australia_anomaly.nc", vars = "t2m") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("t2m", "t2m_a")
```

Base temperature defined as the average of the 12 months previous to the eruption

```{r}
t2m_1y <- strong[, let(base = calculate_tbase(t2_mean, ini_date, period = 12)),
                 by = .(volcano, ini_date)] |>
  _[, get_series(t2_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = t2m_a - base,
          run = "Global")] |> 
  _[]


rbind(t2m_1y|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5)],
      t2m_1y[lag %between% c(-15, 365*1.5+15),
             .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  labs(x = "Months since eruption", y = "Temperature anomaly (C)",
       color = NULL) +
  theme_timeseries

```

```{r}
t2m_1y_tropics <- strong[, let(base = calculate_tbase(tropics_t2_mean, ini_date, period = 12)),
                         by = .(volcano, ini_date)] |>
  _[, get_series(tropics_t2_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = t2m_a - base, 
          run = "Tropics")] |> 
  _[]


rbind(t2m_1y_tropics|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5)],
      t2m_1y_tropics[lag %between% c(-15, 365*1.5+15),
                     .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  labs(x = "Months since eruption", y = "Temperature anomaly (C)",
       color = NULL) +
  theme_timeseries
  
```


```{r}
t2m_1y_australia <- strong[, let(base = calculate_tbase(australia_t2_mean, ini_date, period = 12)),
                           by = .(volcano, ini_date)] |>
  _[, get_series(australia_t2_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = t2m_a - base, 
          run = "Tropics")] |> 
  _[]


rbind(t2m_1y_australia|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5)],
      t2m_1y_australia[lag %between% c(-15, 365*1.5+15),
                       .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  labs(x = "Months since eruption", y = "Temperature anomaly (C)",
       color = NULL) +
  theme_timeseries
```

```{r}

field_base <- purrr::map(Sys.glob("data2/2t_base_composite_all.rds"), readr::read_rds) |> 
  rbindlist() |> 
  setnames("t2m", "t_base")

field_t2m <- readr::read_rds("data2/2t_a6m_4m_composite_all.rds") |> 
  _[, volcano := NULL] |> 
  _[field_base, on = .NATURAL]



field_t2m |> 
  # _[, .(t2m = mean(t2m),
  #                          t_base = mean(t_base)), by = .(latitude, longitude)] |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = t2m - t_base, fill = after_stat(level)),
                    breaks = seq(-2.5, 2.5, 0.25)) +
  scale_fill_divergent_discretised(mid = "white",
                                   labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  # facet_wrap(~period, ncol = 1) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "T (C)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```


```{r}


field_t2m |> 
  # _[, .(t2m = mean(t2m),
  #                          t_base = mean(t_base)), by = .(latitude, longitude)] |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = t2m - t_base, fill = after_stat(level)),
                    breaks = seq(-2, 2, 0.2)) +
  scale_fill_divergent_discretised(mid = "white",
                                   labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  # facet_wrap(~period, ncol = 1) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "T (C)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```


```{r}
field_base <- purrr::map(Sys.glob("data2/2t_base_4mcomposite_*"), readr::read_rds) |>
  rbindlist() |>
  setnames("t2m", "t_base")

field_t2m <- purrr::map(Sys.glob("data2/2t_a6m_4m_composite_*")[1:5], function(f) {
  
  meta <- unglue(f, "data2/2t_{period}composite_{volcano}.rds")
  
  readr::read_rds(f) |>
    _[, period := meta[[1]][["period"]]]
  
}) |> rbindlist() |>
  _[field_base, on = .NATURAL]

field_t2m |>
  # _[, .(t2m = mean(t2m),
  #       t_base = mean(t_base)), by = .(latitude, longitude)] |>
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = t2m - t_base, fill = after_stat(level)),
                    breaks = c(-Inf, seq(-2.5, 2.5, 0.25), Inf)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 30,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE,
          fill = NA, color = "grey20", linewidth = 0.2) +
  geom_point(data =  strong, aes(lon, lat)) +
  facet_wrap(~volcano, ncol = 3) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "T (C)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

### Extremes

```{r}
get_series_np <- function(ini_date, before = 1, after = 3, p = "p95") {
  
  start_time <- ini_date - years(before)
  end_time <- ini_date + years(after)
  temp <- n_px[time %between% c(start_time, end_time) & percentile == p]
  
  list(time = temp$time,
       n_px = temp$n_px)
  
}

calculate_base_np <- function(temperature_px, ini_date, period = 12, p = "p95") {
  
  
  start_period <- lubridate::as_datetime(ini_date) - months(period)
  
  as.numeric(temperature_px[time %between% c(start_period, ini_date) & percentile == p,
                            .(base_px = mean(n_px))])
  
}
```

```{r}

area <- sum(ReadNetCDF("/g/data/w40/pc2687/ve/area.nc", subset = list(latitude = c(-30, 30)), out = "vector")$cell_area)
area_aus <- sum(ReadNetCDF("/g/data/w40/pc2687/ve/area.nc", subset = list(latitude = c(-25, 25),
                                                                          longitude = c(90, 170)), out = "vector")$cell_area)

n_px <- purrr::map(Sys.glob("data2/2t_p*_area.nc"), function(f) {
  
  meta <- unglue::unglue(f, "data2/2t_{p}_area.nc")
  
  ReadNetCDF(f, vars = "t2m") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("t2m", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")

npx_t2 <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := (diff)/area] #km2

rbind(npx_t2 |> 
        _[percentile %in% c("p1", "p99")] |> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))],
      npx_t2[lag %between% c(-15, 365*1.5+15),
             .(t_mean = mean(diff)), by = .(lag, percentile)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")])|> 
  _[lag %between% c(0, 365*1.5)] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area",
       color = NULL) +
  theme_timeseries
```

```{r}
n_px <- purrr::map(Sys.glob("data2/2t_p*_area_tropics.nc"), function(f) {
  
  meta <- unglue::unglue(f, "data2/2t_{p}_area_tropics.nc")
  
  ReadNetCDF(f, vars = "t2m") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("t2m", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")


npx_t2 <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := diff/area] #km2

rbind(npx_t2 |> 
        _[percentile %in% c("p1", "p99")] |> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))],
      npx_t2[lag %between% c(-15, 365*1.5+15),
             .(t_mean = mean(diff)), by = .(lag, percentile)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")])|> 
  _[lag %between% c(0, 365*1.5)] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area",
       color = NULL) +
  theme_timeseries
```

```{r}
n_px <- purrr::map(Sys.glob("/g/data/w40/pc2687/ve/2t_p*_area_australia.nc"), function(f) {
  
  meta <- unglue::unglue(basename(f), "2t_{p}_area_australia.nc")
  
  ReadNetCDF(f, vars = "t2m") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("t2m", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")


npx_t2 <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := diff/area_aus] #km2

rbind(npx_t2 |> 
        _[percentile %in% c("p1", "p99")] |> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))],
      npx_t2[lag %between% c(-15, 365*1.5+15),
             .(t_mean = mean(diff)), by = .(lag, percentile)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")])|> 
  _[lag %between% c(0, 365*1.5)] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area",
       color = NULL) +
  theme_timeseries
```

```{r}
rbind(npx_t2 |> 
        _[percentile %in% c("p1", "p99")] |> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))],
      npx_t2[lag %between% c(-15, 365*1.5+15),
             .(t_mean = mean(diff)), by = .(lag, percentile)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")])|> 
  _[lag %between% c(0, 365*1.5)] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area",
       color = NULL) +
  theme_timeseries

cut_round <- function(x, breaks) {

  labels <- na.omit((breaks + data.table::shift(breaks, -1))/2)
  cuts <- cut(x, breaks = breaks, labels = labels)

  as.numeric(as.character(cuts))
}

rbind(
npx_t2 |> 
  _[, let(month = cut(lag, breaks = seq(-366, 365*2, 30), labels = FALSE)-12)] |> 
  _[] |> 
  _[, .(mean_diff = mean(diff)), by = .(month, id, percentile)],
npx_t2 |> 
  _[, let(month = cut(lag, breaks = seq(-366, 365*2, 30), labels = FALSE)-12)] |> 
  _[] |> 
  _[, .(mean_diff = mean(diff)), by = .(month, percentile)] |> 
  _[, let(id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(month, mean_diff)) +
   geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id)) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Frecuency (%)",
       color = NULL) +
  theme_timeseries
```


```{r}
base <- purrr::map(Sys.glob("data2/2t_p*_base_all.rds"), function(f) {
  
  meta <- unglue(f, "data2/2t_{p}_base_all.rds")
  readr::read_rds(f) |> 
  _[, percentile := meta[[1]][["p"]]]

}) |> 
  rbindlist() |>
  setnames("t2m", "base")


t2_field <- purrr::map(Sys.glob("data2/2t_p*_a6m_4m_all.rds"), function(f) {
  
  meta <- unglue(f, "data2/2t_{p}_a6m_4m_all.rds")
  readr::read_rds(f) |> 
  _[, percentile := meta[[1]][["p"]]]

}) |> 
  rbindlist() |> 
  _[base, on = .NATURAL] |> 
  _[, let(diff = t2m/120 - base/365)]
  

t2_field |> 
  
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = diff*100, fill = after_stat(level)),
                    breaks = seq(-0.6, 0.6, 0.1)*100) +
    geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
    scale_fill_divergent_discretised(mid = "white",
                                   labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Frecuency (%)") +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  theme_minimal() +
  theme(legend.position = "bottom")
```


## Effect on total precipitation

### Anomalies

```{r}
calculate_tbase_tp <- function(temperature_serie, ini_date, period = 12, percentiles = FALSE, p = "p1") {
  
  if(percentiles) {
    
    start_period <- lubridate::as_datetime(ini_date) - months(period)
    
    return(as.numeric(temperature_serie[time %between% c(start_period, ini_date) & percentile == p, 
                                        .(base_t = mean(tp_a))]))
  } else {
    
    start_period <- lubridate::as_datetime(ini_date) - months(period)
    
    return(as.numeric(temperature_serie[time %between% c(start_period, ini_date), 
                                        .(base_t = mean(tp_a))]))
  }
  
}

get_series_tp <- function(t2_mean, ini_date, before = 1, after = 3, percentiles = FALSE, p = "p1") {
  
  start_time <- ini_date - years(before)
  end_time <- ini_date + years(after)
  
  if (percentiles) {
    temp <- t2_mean[time %between% c(start_time, end_time) & percentile == p] 
    
  } else {
    
    temp <- t2_mean[time %between% c(start_time, end_time)]   
  }
  
  list(time = temp$time,
       tp_a = temp$tp_a)
  
}

```


```{r}
tp_mean <-  ReadNetCDF("data2/tp_daily_global_anomaly.nc", vars = "tp") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("tp", "tp_a")

tropics_tp_mean <-  ReadNetCDF("data2/tp_daily_tropics_anomaly.nc", vars = "tp") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("tp", "tp_a")

australia_tp_mean <-  ReadNetCDF("/g/data/w40/pc2687/ve/tp_daily_australia_anomaly.nc", vars = "tp") |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("tp", "tp_a")

tp_1y <- strong[, let(base = calculate_tbase_tp(tp_mean, ini_date, period = 12)),
                by = .(volcano, ini_date)] |>
  _[, get_series_tp(tp_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = tp_a - base,
          run = "Global")] |> 
  _[]


rbind(tp_1y|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5+15)],
      tp_1y[lag %between% c(-15, 365*1.5+15),
            .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean*1000)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id)) +
  labs(x = "Months since eruption", y = "Precipitation anomaly (mm/day)",
       color = NULL) +
  theme_timeseries
```


```{r}
tropics_tp_1y <- strong[, let(base = calculate_tbase_tp(tropics_tp_mean, ini_date, period = 12)),
                        by = .(volcano, ini_date)] |>
  _[, get_series_tp(tropics_tp_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = tp_a - base,
          run = "Tropics")] |> 
  _[]


rbind(tropics_tp_1y|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5+15)],
      tropics_tp_1y[lag %between% c(-15, 365*1.5+15),
                    .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean*1000)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id)) +
  labs(x = "Months since eruption", y = "Precipitation anomaly (mm/day)",
       color = NULL) +
  theme_timeseries
```


```{r}
australia_tp_1y <- strong[, let(base = calculate_tbase_tp(australia_tp_mean, ini_date, period = 12)),
                          by = .(volcano, ini_date)] |>
  _[, get_series_tp(australia_tp_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = tp_a - base,
          run = "Tropics")] |> 
  _[]


rbind(australia_tp_1y|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5)],
      australia_tp_1y[lag %between% c(-15, 365*1.5+15),
                      .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean*1000)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id)) +
  labs(x = "Months since eruption", y = "Precipitation anomaly (mm/day)",
       color = NULL) +
  theme_timeseries
```

```{r}
field_base <- purrr::map(Sys.glob("data2/tp_base_composite_all.rds"), readr::read_rds) |> 
  rbindlist() |> 
  setnames("tp", "t_base")

field_tp <- readr::read_rds("data2/tp_a6m_4m_composite_all.rds") |> 
  # _[, volcano := NULL] |> 
  _[field_base, on = .NATURAL]


field_tp |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = (tp - t_base)*1000, fill = after_stat(level)),
                    breaks = seq(-6, 6, 1)) +
  scale_fill_divergent_discretised(low = "#8c510a", high = "#1b7837", labels = function(x) JumpBy(x, 1, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  # facet_wrap(~period, ncol = 1) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "PP (mm/day)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

```{r}
field_tp |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = (tp - t_base)*1000, fill = after_stat(level)),
                    breaks = seq(-6, 6, 1)) +
  scale_fill_divergent_discretised(low = "#8c510a", high = "#1b7837", labels = function(x) JumpBy(x, 1, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  # facet_wrap(~period, ncol = 1) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "PP (mm/day)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

```{r}
field_base <- purrr::map(Sys.glob("data2/tp_base_a*"), readr::read_rds) |>
  rbindlist() |>
  setnames("tp", "t_base")

field_tp <- purrr::map(Sys.glob("data2/tp_a6m_2m_composite_*")[1:5], function(f) {
  
  meta <- unglue(f, "data2/tp_{period}composite_{volcano}.rds")
  
  readr::read_rds(f) |>
    _[, period := meta[[1]][["period"]]]
  
}) |> rbindlist() |>
  _[field_base, on = .NATURAL]

field_tp |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = (tp - t_base)*1000, fill = after_stat(level)),
                    breaks = seq(-6, 6, 1)) +
  scale_fill_divergent_discretised(low = "#8c510a", high = "#1b7837", labels = function(x) JumpBy(x, 1, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  geom_point(data =  strong, aes(lon, lat)) +
  facet_wrap(~volcano, ncol = 3) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "PP (mm/day)") +
  theme_minimal() +
  theme(legend.position = "bottom")
```
### Extremes


```{r}
n_px <- purrr::map(Sys.glob("data2/tp_p*_area.nc"), function(f) {
  
  meta <- unglue::unglue(f, "data2/tp_{p}_area.nc")
  
  ReadNetCDF(f, vars = "tp") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("tp", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")

npx_tp <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := diff/area] #km2

npx_tp |> 
  _[percentile %in% c("p1", "p99")] |> 
  _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
  _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
  _[lag %between% c(0, 365*1.5)] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  geom_line(data = npx_tp[lag %between% c(-15, 365*1.5+15),
                          .(t_mean = mean(diff)), by = .(lag, percentile)] |>
              _[, let(t_mean = frollmean(t_mean, 30, align = "center"))], aes(y = t_mean)) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1th percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Area (Km2)",
       color = NULL) +
  theme_timeseries
```

```{r}
n_px <- purrr::map(Sys.glob("data2/tp_p*_area_tropics.nc"), function(f) {
  
  meta <- unglue::unglue(f, "data2/tp_{p}_area_tropics.nc")
  
  ReadNetCDF(f, vars = "tp") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("tp", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")

npx_tp <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := diff/area] #km2

npx_tp |> 
  _[percentile %in% c("p1", "p99")] |> 
  _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
  _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
  _[lag %between% c(0, 365*1.5)] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  geom_line(data = npx_tp[lag %between% c(-15, 365*1.5+15),
                          .(t_mean = mean(diff)), by = .(lag, percentile)] |>
              _[, let(t_mean = frollmean(t_mean, 30, align = "center"))], aes(y = t_mean)) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1th percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area (Km2)",
       color = NULL) +
  theme_timeseries
```

```{r}
n_px <- purrr::map(Sys.glob("/g/data/w40/pc2687/ve//tp_p*_area_australia.nc"), function(f) {
  
  meta <- unglue::unglue(basename(f), "tp_{p}_area_australia.nc")
  
  ReadNetCDF(f, vars = "tp") |> 
    _[, percentile := meta[[1]][["p"]]] |> 
    _[, time := as_date(floor_date(time, "day"))] |> 
    _[, let(lat = NULL, lon = NULL)] |> 
    setnames("tp", "n_px")
  
}) |> rbindlist()

percentiles <- c("p1", "p99")

npx_tp <- purrr::map(percentiles, function(i) {
  
  message(i)
  
  strong |>
    _[, let(base = calculate_base_np(n_px, ini_date, p = i)),
      by = .(volcano, ini_date)] |>
    _[,  get_series_np(ini_date, after = 2, p = i),
      by = .(volcano, ini_date)] |>
    
    strong[i = _, on = c("volcano", "ini_date")] |>
    # _[]
    _[, let(diff = n_px - base, 
            id = paste0(volcano, "_", ini_date),
            lag =  as.numeric(difftime(time, ini_date))/86400)] |> 
    _[, percentile := i]
  
}) |> 
  rbindlist() |> 
  _[, diff := diff/area_aus] #km2

npx_tp |> 
  _[percentile %in% c("p1", "p99")] |> 
  _[, .(t_mean = mean(diff)), by = .(lag, id, percentile)] |> 
  _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
  _[lag %between% c(0, 365*1.5)] |> 
  ggplot(aes(lag/30, t_mean)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  geom_line(data = npx_tp[lag %between% c(-15, 365*1.5+15),
                          .(t_mean = mean(diff)), by = .(lag, percentile)] |>
              _[, let(t_mean = frollmean(t_mean, 30, align = "center"))], aes(y = t_mean)) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1th percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Change in area (Km2)",
       color = NULL) +
  theme_timeseries

rbind(
npx_tp |> 
  _[, let(month = cut(lag, breaks = seq(-366, 365*2, 30), labels = FALSE)-12)] |> 
  _[] |> 
  _[, .(mean_diff = mean(diff)), by = .(month, id, percentile)],
npx_tp |> 
  _[, let(month = cut(lag, breaks = seq(-366, 365*2, 30), labels = FALSE)-12)] |> 
  _[] |> 
  _[, .(mean_diff = mean(diff)), by = .(month, percentile)] |> 
  _[, let(id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(month, mean_diff)) +
   geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id)) +
  scale_y_continuous(labels = scales::percent_format()) +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  labs(x = "Months since eruption", y = "Frecuency (%)",
       color = NULL) +
  theme_timeseries

```

```{r}
base <- purrr::map(Sys.glob("data2/tp_p*_base_all.rds"), function(f) {
  
  meta <- unglue(f, "data2/tp_{p}_base_all.rds")
  readr::read_rds(f) |> 
  _[, percentile := meta[[1]][["p"]]]

}) |> 
  rbindlist() |>
  setnames("tp", "base")


tp_field <- purrr::map(Sys.glob("data2/tp_p*_a6m_4m_all.rds"), function(f) {
  
  meta <- unglue(f, "data2/tp_{p}_a6m_4m_all.rds")
  readr::read_rds(f) |> 
  _[, percentile := meta[[1]][["p"]]]

}) |> 
  rbindlist() |> 
  _[base, on = .NATURAL] |> 
  _[, let(diff = tp/120 - base/365)]
  

tp_field |> 
  
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = diff*100, fill = after_stat(level)),
                    breaks = seq(-0.2, 0.2, 0.025)*100) +
    geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
    scale_fill_divergent_discretised(mid = "white",
                                   labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Frequency (%)") +
  facet_wrap(~percentile, labeller = labeller(percentile = c("p1" = "1st percentile",
                                                             "p99" = "99th percentile"))) +
  theme_minimal() +
  theme(legend.position = "bottom")
```


## MSE

```{r}
calculate_base_mse <- function(temperature_serie, ini_date, period = 12) {
  
  start_period <- lubridate::as_date(ini_date) - months(period)
  
  temperature_serie[time %between% c(start_period, ini_date), 
                    .(base = mean(value)), by = variable]
}

get_series_mse <- function(mse_serie, ini_date, var, before = 1, after = 3) {
  # browser()
  start_time <- ini_date - years(before)
  end_time <- ini_date + years(after)
  
  temp <- mse_serie[time %between% c(start_time, end_time) & variable == var] 
  
  
  list(time = temp$time,
       variable = temp$variable,
       value = temp$value)
  
}

rad_serie <- readr::read_rds("data2/rad_mean_daily_regions.rds")$austrlia |> 
  _[, let(time = as_date(time))] |> 
  # setnames(old = c("ssr", "tsr"), new = c("sw", "lw")) |> 
  melt(id.vars = "time") 


mse_serie <- readr::read_rds("data2/mse_mean_daily_regions_wg.rds")$austrlia |> 
  _[, let(time = as_date(time),
          vdh_dx = -vdh_dx,
          res = dh_dp + vdh_dx - fnet)] |> 
  melt(id.vars = "time") |> 
  rbind(i = _, rad_serie)

base <- strong[, calculate_base_mse(mse_serie, ini_date, period = 12),
               by = .(volcano, ini_date)]

mse_anomalia <- strong[, calculate_base_mse(mse_serie, ini_date, period = 12),
                       by = .(volcano, ini_date)] |> 
  _[, get_series_mse(mse_serie, ini_date, variable, before = 1, after = 2), by = .(volcano, ini_date, variable)] |>
  base[i = _, on = c("volcano", "ini_date", "variable")] |>
  # _[]
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = value - base)] |>
  _[]


latex <- c("fnet" = "FNET", 
           "rad" = "Rad", 
           "slhf" = "Latent_heat", 
           "sshf" = "Sensible_heat", 
           "dh_dp" = latex2exp::TeX("$ \\omega \\partial h / \\partial p$"),
           "vdh_dx" = latex2exp::TeX("$-v\\nabla h$"),
           "Res" = "Residual",
           "sw" = "SW",
           "lw" = "LW")
mse_anomalia <- mse_anomalia |> 
  # _[variable %in% c("dh_dp", "vdh_dx")] |> 
  _[, variable2 := factor(variable)] 

levels(mse_anomalia$variable2) <- latex
```


```{r}

rbind(
mse_anomalia |> 
  _[variable %in% c("fnet", "dh_dp", "vdh_dx")] |> 
  _[, .(mean_diff = mean(diff)), by = .(lag, id, variable2)] |> 
  _[, let(mean_diff = frollmean(mean_diff, 30, align = "center"))],
  mse_anomalia[lag %between% c(-30, 365*1.4) & variable %in% c("fnet", "dh_dp", "vdh_dx"),
                                .(mean_diff = mean(diff)), by = .(lag, variable2)] |>
              _[, let(mean_diff = frollmean(mean_diff, 30, align = "center"),
                      id = "Mean")]) |> 
  _[lag %between% c(-10, 365*1.4)] |> 
  _[, let(variable2 = forcats::fct_relevel(variable2, "FNET", after = Inf))] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, mean_diff)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  facet_wrap(~variable2, ncol = 1, labeller = labeller(variable2 = label_parsed),
             strip.position = "right") +
  labs(x = "Months since eruption", y = "(Wm-2)",
       color = NULL) +
  theme_timeseries
```


```{r}
rbind(
mse_anomalia |> 
  _[variable %in% c("sshf", "slhf", "rad", "sw", "lw")] |> 
  _[, .(mean_diff = mean(diff)), by = .(lag, id, variable2)] |> 
  _[, let(mean_diff = frollmean(mean_diff, 30, align = "center"))],
  mse_anomalia[lag %between% c(-30, 365*1.4) & variable %in% c("sshf", "slhf", "rad", "sw", "lw"),
                                .(mean_diff = mean(diff)), by = .(lag, variable2)] |>
              _[, let(mean_diff = frollmean(mean_diff, 30, align = "center"),
                      id = "Mean")]
) |> 
  _[lag %between% c(-10, 365*1.4)] |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, mean_diff)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id, linewidth = id == "Mean")) +
  facet_wrap(~factor(variable2, c("Rad", "SW", "LW", "Latent_heat", "Sensible_heat")), 
             ncol = 2, 
             dir = "v",
             labeller = labeller(variable2 = label_parsed)) +
  labs(x = "Months since eruption", y = "(Wm-2)",
       color = NULL) +
  theme_timeseries
```

```{r}
mse_anomalia |> 
  _[variable %in% c("rad", "sw", "lw")] |> 
  _[, .(mean_diff = mean(diff)), by = .(lag, id, variable2)] |> 
  _[, let(mean_diff = frollmean(mean_diff, 30, align = "center"))] |> 
  _[lag %between% c(-10, 365*1.4)] |> 
  # _[, let(variable2 = forcats::fct_relevel(variable2, c("Rad", "SW", "LW", "Latent_heat", "Sensible_heat")))] |>
  ggplot(aes(lag/30, mean_diff)) +
  geom_hline(yintercept = 0, color =  "grey") +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = id), alpha = 0.5) +
  geom_line(data = mse_anomalia[lag %between% c(-30, 365*1.4) & variable %in% c("rad", "sw", "lw"),
                                .(t_mean = mean(diff)), by = .(lag, variable2)] |>
              _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
              _[lag %between% c(-10, 365*1.4)], aes(y = t_mean)) +
  # geom_smooth() +
  guides(color = guide_legend(nrow = 2)) +
  facet_wrap(~factor(variable2, c("Rad", "SW", "LW")), 
             ncol = 2, 
             dir = "v",
             labeller = labeller(variable2 = label_parsed)) +
  scale_x_continuous(breaks = seq(0, 12, 2)) +
  scale_color_brewer(palette = "Dark2") +
  coord_cartesian(expand = FALSE, xlim = c(0, 15)) +
  labs(x = "Months since eruption", y = "(Wm-2)",
       color = NULL) +
  theme_minimal() +
  theme(legend.position = "bottom", legend.box="vertical")
```


```{r}


# 
# mse_anomalia[volcano == "Pinatubo"] |> 
#   ggplot(aes(lag, value)) +
#   geom_line(aes(color = variable), alpha = 0.1) 
#   geom_line(aes(y = base, color = variable))


mse_anomalia[lag %between% c(-100, 380*1),
             .(mse_mean = mean(value)), by = .(lag, variable)] |>
  _[, let(mse_mean = frollmean(mse_mean, 30, align = "center"))] |> 
  _[lag %between% c(0, 360*1)] |>
  _[, budget := fifelse(variable %in% c("fnet", "dh_dp", "vdh_dx", "res"), "budget", "secondary")] |> 
  # _[]
  ggplot(aes(lag/30, mse_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = variable, linetype = budget)) +
  scale_color_manual(values = c("fnet" = "#a50f15", "rad" = "#de2d26", "slhf" = "#fb6a4a", "sshf" = "#fcae91", 
                                "dh_dp" = "cyan4", "vdh_dx" = "steelblue",
                                "res" = "grey40")) +
  scale_x_continuous(breaks = seq(0, 12, 2)) +
  scale_linetype(guide = NULL) +
  labs(x = "Months since eruption", y = "Budget components (Wm-2)", color = NULL) +
  theme_minimal() +
  theme(legend.position = "bottom", legend.box="vertical",
        panel.grid.minor = element_line(color = "white"), aspect.ratio = 1/3,
        panel.grid.major.y = element_line(color = "white"))

mse_anomalia[lag %between% c(-100, 380*1),
             .(mse_mean = mean(diff)), by = .(lag, variable)] |>
  _[, let(mse_mean = frollmean(mse_mean, 30, align = "center"))] |> 
  _[lag %between% c(0, 360*1)] |>
  _[, budget := fifelse(variable %in% c("fnet", "dh_dp", "vdh_dx", "res"), "budget", "secondary")] |> 
  _[variable != "res"] |> 
  ggplot(aes(lag/30, mse_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color = "grey20") +
  geom_line(aes(color = variable, linetype = budget)) +
  scale_color_manual(values = c("fnet" = "#a50f15", "rad" = "#de2d26", "slhf" = "#fb6a4a", "sshf" = "#fcae91", 
                                "dh_dp" = "cyan4", "vdh_dx" = "steelblue",
                                "res" = "grey40")) +
  scale_x_continuous(breaks = seq(0, 12, 2)) +
  scale_linetype(guide = NULL) +
  labs(x = "Months since eruption", y = "Budget components (Wm-2)", color = NULL) +
  theme_minimal() +
  theme(legend.position = "bottom", legend.box="vertical",
        panel.grid.minor = element_line(color = "white"), aspect.ratio = 1/3,
        panel.grid.major.y = element_line(color = "white"))
  ```

```{r}
mse_anomalia |> 
  _[, .(mse_mean = mean(value)), by = .(lag, variable)] |> 
  _[, budget := fifelse(variable %in% c("fnet", "dh_dp", "vdh_dx", "res"), "budget", "secondary")] |> 
  _[budget == "secondary"] |> 
  ggplot(aes(lag/30, mse_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = variable, linetype = budget)) +
  scale_color_manual(values = c("fnet" = "#a50f15", "rad" = "#de2d26", "slhf" = "#fb6a4a", "sshf" = "#fcae91", 
                                "dh_dp" = "cyan4", "vdh_dx" = "steelblue",
                                "res" = "grey40")) +
  scale_x_continuous(breaks = seq(0, 12, 2)) +
  scale_linetype(guide = NULL) +
  labs(x = "Months since eruption", y = "Budget components (Wm-2)", color = NULL) +
  theme_minimal()

mse_anomalia |> 
  _[, .(mse_mean = mean(diff)), by = .(lag, variable)] |> 
  _[, budget := fifelse(variable %in% c("fnet", "dh_dp", "vdh_dx", "res"), "budget", "secondary")] |> 
  _[budget == "secondary"] |> 
  ggplot(aes(lag/30, mse_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_line(aes(color = variable, linetype = budget)) +
  scale_color_manual(values = c("fnet" = "#a50f15", "rad" = "#de2d26", "slhf" = "#fb6a4a", "sshf" = "#fcae91", 
                                "dh_dp" = "cyan4", "vdh_dx" = "steelblue",
                                "res" = "grey40")) +
  scale_x_continuous(breaks = seq(0, 12, 2)) +
  scale_linetype(guide = NULL) +
  facet_wrap(~variable, ncol = 1) +
  labs(x = "Months since eruption", y = "Budget components (Wm-2)", color = NULL) +
  theme_minimal()
```

```{r}
calculate_tbase_w <- function(temperature_serie, ini_date, period = 12) {
  
  start_period <- lubridate::as_datetime(ini_date) - months(period)
  
  return(as.numeric(temperature_serie[time %between% c(start_period, ini_date), 
                                      .(base_t = mean(w_a))]))
  
}

get_series_w <- function(t2_mean, ini_date, before = 1, after = 3) {
  
  start_time <- ini_date - years(before)
  end_time <- ini_date + years(after)
  
  temp <- t2_mean[time %between% c(start_time, end_time)]   
  
  list(time = temp$time,
       w_a = temp$w_a)
  
}

```


```{r}
w_mean <- readr::read_rds("data2/w_mean_daily_regions.rds")$austrlia |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("w", "w_a")

tropics_w_mean <- readr::read_rds("data2/w_mean_daily_regions.rds")$tropics |> 
  _[, time := floor_date(time, "day")] |> 
  _[, time := as_date(time, tz = "UTC")] |> 
  setnames("w", "w_a")

w_1y <- strong[, let(base = calculate_tbase_w(w_mean, ini_date, period = 12)),
                by = .(volcano, ini_date)] |>
  _[, get_series_w(w_mean, ini_date, before = 1, after = 2), by = .(volcano, ini_date)] |> 
  strong[i = _, on = c("volcano", "ini_date")] |> 
  _[, let(id = paste0(volcano, "_", ini_date),
          lag =  as.numeric(difftime(time, ini_date)/86400),
          diff = w_a - base,
          run = "Global")] |>
  _[]


rbind(w_1y|> 
        _[, .(t_mean = mean(diff)), by = .(lag, id, run)] |> 
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"))] |> 
        _[lag %between% c(0, 365*1.5+15)],
      w_1y[lag %between% c(-15, 365*1.5+15),
            .(t_mean = mean(diff)), by = .(lag, run)] |>
        _[, let(t_mean = frollmean(t_mean, 30, align = "center"),
                id = "Mean")]) |> 
  _[, let(id = forcats::fct_relevel(id, "Mean", after = Inf))] |> 
  ggplot(aes(lag/30, t_mean)) +
  annotate("rect", xmin = 4, xmax = 8, ymin = -Inf, ymax = Inf, alpha = .1) +
  geom_hline(yintercept = 0, color =  "grey") +
  geom_line(aes(color = id)) +
  labs(x = "Months since eruption", y = "Omega (Pas-1)",
       color = NULL) +
  theme_timeseries

```



```{r}



field_base <- rbind(
  readr::read_rds("data2/w_base_composite_all.rds") |> 
    _[, let(level = NULL,
            variable = "w",
            base = w)] |> 
    _[, .(longitude, latitude, variable, base)],
  
  readr::read_rds("data2/mse_base_composite_all.rds") |> 
    _[, vdh_dx := -vdh_dx] |> 
    melt(id.vars = c("longitude", "latitude")) |> 
    setnames("value", "base"),
  
  readr::read_rds("data2/rad_base_composite_all.rds") |> 
   melt(id.vars = c("longitude", "latitude")) |> 
    setnames("value", "base") 
  )

field_mse <-  rbind(
  readr::read_rds("data2/w_a6m_4m_composite_all.rds") |> 
    _[, let(level = NULL,
            variable = "w",
            value = w)] |> 
    _[, .(longitude, latitude, variable, value)],
  
  readr::read_rds("data2/mse_a6m_4m_composite_all.rds") |> 
    _[, vdh_dx := -vdh_dx] |> 
    melt(id.vars = c("longitude", "latitude")),
  
   readr::read_rds("data2/rad_a6m_4m_composite_all.rds") |> 
    # _[, vdh_dx := -vdh_dx] |> 
    melt(id.vars = c("longitude", "latitude"))
  )


field_mse <- field_mse |> 
  _[field_base, on = .NATURAL]
```


```{r}
field_mse |> 
  _[variable %in% c("rad", "sw", "lw", "slhf", "sshf")] |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = value - base, fill = after_stat(level))) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 5, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey", linewidth = 0.2) +
  facet_wrap(~variable, labeller = labeller(variable = c("rad" = "RAD", "sw" = "SW", "lw" = "LW",
                                                         "slhf" = "Latent heat", "sshf" = "Sensible heat"))) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "Wm-2") +
  theme_minimal() +
  theme(legend.position = "bottom")


field_mse |> 
  _[variable %in% c("rad", "sw", "lw", "slhf", "sshf")] |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = value - base, fill = after_stat(level)),
                    breaks = seq(-60, 80, 5)) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 5, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey30", linewidth = 0.2) +
  facet_wrap(~variable, labeller = labeller(variable = c("rad" = "RAD", "sw" = "SW", "lw" = "LW",
                                                         "slhf" = "Latent heat", "sshf" = "Sensible heat"))) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Wm-2") +
  theme_minimal() +
  theme(legend.position = "bottom")


field_mse |> 
  # _[variable %in% c("rad", "sw", "lw", "slhf", "sshf")] |> 
  _[variable %in% c("sw", "lw")] |> 
  ggplot(aes(longitude, latitude)) +
  geom_point(aes(color = value - base)) +
  # geom_contour_fill(aes(z = value - base, fill = after_stat(level))) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 5, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey30", linewidth = 0.2) +
  facet_wrap(~variable, labeller = labeller(variable = c("rad" = "RAD", "sw" = "SW", "lw" = "LW",
                                                         "slhf" = "Latent heat", "sshf" = "Sensible heat"))) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Wm-2") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

```{r}
field_mse |> 
  _[variable %in% c("w")] |> 
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = value - base, fill = after_stat(level)),
                    breaks = c(seq(-0.1, 0.1, 0.02), Inf)) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 1, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey20", linewidth = 0.2) +
  # facet_wrap(~variable, labeller = labeller(variable = c("fnet" = "FNET", "rad" = "RAD",
  #                                                        "slhf" = "Latent heat", "sshf" = "Sensible heat"))) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Pas-1") +
  theme_minimal() +
  theme(legend.position = "bottom")
```



```{r}
# labels <- c("fnet" = "FNET", "dh_dp" = "omega delta p", "vdh_dx" = "v delta h delta x")
latex <- c("fnet" = "FNET",
           "dh_dp" = latex2exp::TeX("$ \\omega \\partial h / \\partial p$"),
           "vdh_dx" = latex2exp::TeX("$v\\nabla h$"))

data <- field_mse |> 
  _[variable %in% c("fnet", "dh_dp", "vdh_dx")] |> 
  _[, variable2 := factor(variable)] 

levels(data$variable2) <- latex

data |>  
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = value - base, fill = after_stat(level)),
                    breaks = c(-Inf, seq(-90, 90, 10), Inf)) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey", linewidth = 0.2) +
  facet_wrap(~variable2, labeller = labeller(variable2 = label_parsed)) +
  coord_sf(expand = FALSE, ylim = c(-60, 60)) +
  labs(x = NULL, y = NULL, fill = "Wm-2") +
  theme_minimal() +
  theme(legend.position = "bottom")


data |>  
  ggplot(aes(longitude, latitude)) +
  geom_contour_fill(aes(z = value - base, fill = after_stat(level)),
                    breaks = c(-Inf, seq(-90, 90, 10), Inf)) +
  # scale_fill_viridis_d(guide = guide_colorsteps(barwidth = 25,
  #                                                         barheight = 0.5)) +
  scale_fill_divergent_discretised(labels = function(x) JumpBy(x, 2, fill = ""),
                                   guide = guide_colorbar(barwidth = 25,
                                                          barheight = 0.5)) +
  geom_sf(data = map, inherit.aes = FALSE, 
          fill = NA, color = "grey30", linewidth = 0.2) +
  facet_wrap(~variable2, labeller = labeller(variable2 = label_parsed)) +
  coord_sf(expand = FALSE, ylim = c(-25, 25), xlim = c(90, 170)) +
  labs(x = NULL, y = NULL, fill = "Wm-2") +
  theme_minimal() +
  theme(legend.position = "bottom")
```